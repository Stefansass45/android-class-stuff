<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/main"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/imageViewPreview"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/ic_launcher_background"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp" />

    <Button
        android:id="@+id/buttonSelectImage"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Select Sticker Image"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp" />

    <EditText
        android:id="@+id/editTextStickerName"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:hint="Enter something"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp" />

    <EditText
        android:id="@+id/ediTextStickerCategory"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:hint="Enter more"
        android:layout_gravity="center_horizontal" />

    <Button
        android:id="@+id/buttonUploadSticker"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Upload Sticker"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:id="@+id/progressBar"
        tools:listitem="@layout/item_sticker"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvSticker"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        />

</LinearLayout>